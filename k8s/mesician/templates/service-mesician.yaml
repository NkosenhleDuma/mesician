apiVersion: v1
kind: Service
metadata:
  name: mesician
  namespace: {{ .Release.Namespace }}
spec:
  type: ClusterIP
  selector:
    app: mesician
  ports:
    - name: http
      port: 3000
      targetPort: http
